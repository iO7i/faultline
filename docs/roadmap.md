# Roadmap

Deferred seams include DEXPI ingestion through an `EngineeringSourceAdapter`, durable-host adapters, richer temporal monitors, and additional simulators. None is required to prove this foundation's two invariants.
