# Systematic counterexample engine

Bounded deterministic exploration for the synthetic execution IR and its six Authority-Effect Linearizability invariants. It is simulator-only and emits replayable JSON counterexample capsules; it does not connect to providers, LLMs, credentials, or physical systems.
